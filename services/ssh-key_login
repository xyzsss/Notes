# env : bash

# new system add user use ssh-key login


USER_NAME='xxxxxx'

useradd USER_NAME

su - USER_NAME
# auth
ssh-keygen -t rsa -b 2048 -f id_rsa  -N ''
cd .ssh
touch authorized_keys
chmod 600 authorized_keys
# echo "xxx" >> authorized_keys
exit
